namespace PhoneHolder.Model
{
    /// <summary>
    /// Класс констант для держателя телефона.
    /// </summary>
    public static class Constants
    {
        public const float MIN_HEIGHT = 50.0f; // Минимальная высота держателя (мм)
        public const float MAX_HEIGHT = 150.0f; // Максимальная высота держателя (мм)

        public const float MIN_TILT_ANGLE = 0.0f; // Минимальный угол наклона (градусы)
        public const float MAX_TILT_ANGLE = 90.0f; // Максимальный угол наклона (градусы)

        public const float MIN_BASE_WIDTH = 30.0f; // Минимальная ширина основания (мм)
        public const float MAX_BASE_WIDTH = 100.0f; // Максимальная ширина основания (мм)

        public const float MIN_CLAMP_WIDTH = 10.0f; // Минимальная ширина зажима (мм)
        public const float MAX_CLAMP_WIDTH = 50.0f; // Максимальная ширина зажима (мм)

        public const float MIN_HOLDER_WIDTH = 20.0f; // Минимальная ширина держателей (мм)
        public const float MAX_HOLDER_WIDTH = 80.0f; // Максимальная ширина держателей (мм)
    }
}
